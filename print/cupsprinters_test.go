package print

/*
func TestNewPrintersFromGroups(t *testing.T) {
	msg := createTestPrintersResponse()
	groups, err := createGroupsFromMessage(msg)
	assert.Nil(t, err)

	p := newPrintersFromGroups(groups)
	assert.Equal(t, "Printer1", p.Printers[0].Name())
}

func TestGetPrinterIndexByName(t *testing.T) {
	msg := createTestPrintersResponse()
	groups, err := createGroupsFromMessage(msg)
	assert.Nil(t, err)

	p := newPrintersFromGroups(groups)
	index, err := p.getPrinterIndexByName("Printer1")
	assert.Nil(t, err)
	assert.Equal(t, 0, index)

	index, err = p.getPrinterIndexByName("Printer2")
	assert.Nil(t, err)
	assert.Equal(t, 1, index)

	index, err = p.getPrinterIndexByName("Printer16")
	assert.NotNil(t, err)
	assert.Equal(t, -1, index)
}
*/
